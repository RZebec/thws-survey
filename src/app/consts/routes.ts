export enum routes {
  LOGIN = '/login',
  KANBAN = '/kanban',
  SIGNUP = '/signUp',
  SURVEY = '/survey',
  TODO = '/todo',
  TERMS = '/terms',
  STEPNAVIGATOR = '/step-navigator',
  DONE = '/done',
}
